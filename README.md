# Stock-Price-Prediction-Model
For complete detail regarding lib's and tech refer to - StockReport.pdf
to run -
1. clone the repo
2. run the ipynb choose
3. it will create a .keras trained model to do the prediction stuff
4. then add the address to the .keras file in the app.py
5. run the app.py if faces some sort of error try running cmd using command "streamlit run app.py"
